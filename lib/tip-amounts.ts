/**
 * チップとして選べる金額。
 *
 * 【なぜサーバーアクションのファイルから分けているか】
 * "use server" を付けたファイルからは、非同期関数しかエクスポートできない。
 * 定数を一緒にエクスポートすると実行時にエラーとなり、
 * この定数を読み込んでいる商品ページ全体が表示できなくなる。
 * （実際にそれで商品ページが開けなくなった）
 *
 * 任意入力にせず選択式にしているのは、ブラウザから任意の数値を
 * 送れる状態にすると、1円チップや極端な高額での誤操作を招くため。
 */
export const TIP_AMOUNTS = [300, 500, 1000, 3000] as const;
export type TipAmount = (typeof TIP_AMOUNTS)[number];
