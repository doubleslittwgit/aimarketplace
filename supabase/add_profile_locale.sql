-- ============================================================
-- profiles.locale
-- ============================================================
-- 「その人が出品した時に使っていた言語」でメール通知を送るための、
-- ユーザーごとの言語設定。出品するたびに、その時のページのロケールで
-- 上書き更新する（app/submit/actions.ts 参照）。
-- ============================================================

alter table public.profiles add column if not exists locale text not null default 'ja';
