-- ============================================================
-- ツールにサムネイル画像を追加
-- ============================================================
-- 既存の schema.sql / storage.sql / functions.sql / grants.sql の
-- 後に実行してください（追加のみで、既存データは壊しません）。
-- ============================================================

alter table public.tools
  add column if not exists thumbnail_url text;
